fastapi==0.117.1
uvicorn[standard]==0.37.0
pydantic==2.11.9
cryptography==45.0.5
pywinrm==0.5.0
pypsrp[credssp,kerberos]==0.8.1
requests==2.32.4
psutil==7.0.0
wakeonlan==3.1.0
mac-vendor-lookup==0.1.15
dnspython==2.7.0
ntplib==0.4.0
pysnmp==7.1.27
pywin32==311; platform_system == "Windows"

# Test and legacy/optional deps live in requirements-dev.txt so a production
# install (PyInstaller / fresh ops box) doesn't pull pytest, customtkinter, etc.
# Install with: pip install -r requirements.txt -r requirements-dev.txt